# configure

Reconciles privileged configuration for deployed platform services.

## Commands

Use `k configure --help` to list the available commands.

Each command changes a live service. Review its help and confirm the target
before running it.
