apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: fe
spec:
  hostnames:
    - scg.skku.ac.kr
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /
      backendRefs:
        - name: fe
          port: 80
