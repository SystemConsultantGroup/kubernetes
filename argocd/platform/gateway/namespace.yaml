apiVersion: v1
kind: Namespace
metadata:
  name: gateway-system
  labels:
    # Envoy Gateway's host-networked data plane binds public listener ports.
    pod-security.kubernetes.io/enforce: privileged
    pod-security.kubernetes.io/enforce-version: latest
