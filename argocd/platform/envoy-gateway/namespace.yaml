apiVersion: v1
kind: Namespace
metadata:
  name: envoy-gateway-system
  labels:
    # The managed Envoy data plane is host-networked to preserve the current
    # node-address exposure and listener ports.
    pod-security.kubernetes.io/enforce: privileged
    pod-security.kubernetes.io/enforce-version: latest
